# Medical Report Analysis — Dataset Download Dependencies
# Install with: pip install -r requirements-datasets.txt
#
# Keep this SEPARATE from requirements.txt to avoid
# polluting the inference environment.

datasets>=2.18.0
kaggle>=1.6.0
huggingface_hub>=0.22.0
pandas>=2.0.0
tqdm>=4.66.0
